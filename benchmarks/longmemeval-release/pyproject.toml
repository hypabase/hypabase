[project]
name = "longmemeval-bench"
version = "0.1.0"
description = "LongMemEval benchmark for Hypabase Memory"
requires-python = ">=3.11"
dependencies = [
    "hypabase",
    "pydantic-ai>=1.73.0",
    "anthropic>=0.86.0",
    "huggingface-hub>=0.20.0",
    "tqdm>=4.67.0",
    "fastembed>=0.7",
]

[tool.uv.sources]
hypabase = { path = "../..", editable = true }
